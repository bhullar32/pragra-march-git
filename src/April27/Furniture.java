package April27;

public interface Furniture {

    //By default fields are public static final
    int x=10;

    //by default methods are public abstract
    //methods cannot have body
    //Interfaces cannot have contstructors as object cannot be created
    //But constructor can be created for abstract classes,

    void show();
}
